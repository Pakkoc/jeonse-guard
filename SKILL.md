---
name: jeonse-guard
description: 전세 계약 전 안전진단 + 계약 후 보증금 워치독. 지번 주소 하나로 매매·전세 실거래 12개월(전세가율 산출), 건축물대장 표제부(근린생활시설 감지)를 교차 조회해 사실·검토신호·직접확인 체크리스트 3층 리포트를 만든다. 사용자가 전세, 전셋집, 전세 계약, 보증금, 깡통전세, 전세사기, 전세가율, 근생빌라, 빌라 전세, 임대차 계약 검토, "이 집 전세 괜찮아?", "보증금 떼일까 무서워"를 언급하거나 특정 주소·매물의 전세 계약을 고민할 때 — 명시적으로 "진단"이라 말하지 않아도 — 반드시 이 스킬을 사용한다. 단순 매매 시세 조회는 real-estate-search, 등기부 열람은 iros-registry-automation이 더 적합하다.
license: MIT
metadata:
  category: real-estate
  locale: ko-KR
  phase: v1
---

# 전세 안전진단 (jeonse-guard)

## What this skill does

지번 주소 하나로 전세 계약 전 안전진단 리포트를 만든다. 무료 공공데이터를 교차 조회하며, 어느 항목이 실패해도 나머지로 리포트를 완성한다(실패 강등).

| 확인 항목 | 데이터 | 산출 |
|---|---|---|
| 전세가율 | 국토교통부 매매·전월세 실거래 12개월 | 보증금 ÷ 매매 중위가 (단지·면적대 매칭) |
| 건물 정체 | 건축물대장 표제부 | 주용도가 근린생활시설인지(근생빌라), 사용승인일·층수 |
| 검토 신호 | 위 데이터 | 근거 있을 때만 발화하는 신호 5종 |
| 자동화 불가 항목 | — | 등기부·선순위보증금·전입세대 등 직접 확인 체크리스트 |

계약 후에는 워치독 모드로 신규 실거래·전세가율 80% 돌파·대장 변동을 매일 감시할 수 있다.

## When to use

- "이 집 전세로 들어가도 괜찮아?" — 주소를 받았을 때
- "보증금 3억인데 깡통전세 아닌지 봐줘"
- "이 빌라 근생인지 확인해줘"
- 전세 계약을 앞두고 무엇을 확인해야 하는지 물을 때

## When not to use

- 매매 시세·실거래 단순 조회 (real-estate-search)
- 등기부등본 열람·발급 (iros-registry-automation — 근저당은 이 스킬이 못 본다)
- 월세·상가 임대차 (미지원)

## Prerequisites

- `python3` 3.11 이상, 인터넷 연결
- 사용자 API 키 불필요 (실거래·지오코딩은 k-skill-proxy 경유)
- 선택: `DATA_GO_KR_API_KEY` 환경변수 — 있으면 건축물대장(근생 감지) 섹션 활성화. 없으면 해당 섹션만 "확인 불가"로 강등되고 나머지는 정상 동작한다.

## Install

이미 설치돼 있는지부터 확인한다:

```bash
command -v jeonse-guard || ls "${TMPDIR:-/tmp}/jeonse-guard-venv/bin/jeonse-guard" 2>/dev/null
```

없으면 임시 가상환경에 설치한다 (macOS 시스템 python은 PEP 668로 `pip install --user`가 막히므로 venv가 안전하다):

```bash
python3 -m venv "${TMPDIR:-/tmp}/jeonse-guard-venv"
"${TMPDIR:-/tmp}/jeonse-guard-venv/bin/pip" -q install "git+https://github.com/Pakkoc/jeonse-guard.git"
JG="${TMPDIR:-/tmp}/jeonse-guard-venv/bin/jeonse-guard"
```

의존성이 0(표준 라이브러리만)이라 수 초면 끝난다. 이하 `$JG`는 설치된 실행 파일 경로다 (`command -v jeonse-guard`가 있으면 그것을 쓴다).

## Inputs to gather

실행 전 사용자에게서 모을 것 (부족하면 물어본다 — 단, 주소만 있어도 실행은 가능하다):

- **주소 (필수)**: 지번 주소 (예: "서울 마포구 성산동 200-1"). 도로명 주소만 알면 그대로 시도해도 되고, 주소가 하나로 확정되지 않으면 도구가 후보를 보여주며 중단하니 그때 지번을 되물어라.
- **보증금 (권장)**: 만원 단위 정수로 변환한다 — "3억5천" → 35000, "1억2,500" → 12500. 없으면 전세 실거래 중위값으로 대신 산출된다.
- **단지/건물명, 전용면적(㎡)**: 있으면 매칭 정확도가 크게 오른다 (아파트는 단지명, 빌라는 생략 가능).

## Workflow

1. 설치 확인 → 없으면 Install 절차 실행.
2. 입력 수집 → 진단 실행 (약 30초, 프록시 24회 호출):

```bash
$JG scan "서울 마포구 성산동 200-1" --name 성산시영 --area 50 --deposit 30000
```

옵션: `--asset villa|officetel` (기본 apartment), `--months N` (기본 12), `--out DIR` (리포트 저장 위치 — 기본이 실행 위치 기준 `./reports`이므로 명시 권장), `--json` (구조화 출력).

자산 유형을 모르면 apartment로 실행하고, 사용자 표현에 "빌라"·"오피스텔" 단서가 있거나 결과 표본이 0이면 `--asset villa`(연립다세대)로 재시도한다.

3. stdout의 Markdown 리포트를 읽고 사용자에게 **해석해서** 전달한다:
   - 전세가율과 산출 근거(표본 수·매칭 범위)를 수치 그대로 인용
   - 발화한 검토 신호는 근거(basis)와 함께 설명
   - **직접 확인 체크리스트를 반드시 함께 전달** — 등기부 근저당·선순위보증금·전입세대 열람·보증보험·임대인 납세증명은 이 도구가 못 보는 영역임을 명확히
4. 계약 예정/기존 세입자라면 워치독을 제안한다 (아래).

## Interpreting results — wording rules

이 도구는 판정하지 않는다. 사용자에게 전달할 때도 같은 원칙을 지킨다:

- 금지: "안전한 매물입니다", "사기입니다", "위험 확정", "깡통전세 확정"
- 권장: "전세가율 85.5%로 통상 주의선(80%)을 넘습니다", "대장 주용도가 근린생활시설로 확인됩니다 — 보증보험 가입과 대출에 제약이 있을 수 있으니 확인이 필요합니다", "신호가 없다는 것이 문제 없음을 뜻하지는 않습니다"
- **매칭 범위를 반드시 확인하라.** 리포트의 "매칭 범위"가 `지역구`(또는 `면적`)로 강등돼 있으면 그 전세가율은 해당 건물이 아니라 **동네 전체의 참고치**다 — 건물의 지표처럼 전달하지 말고, 단지/건물명과 전용면적을 받아 재실행을 권하라. `단지+면적`·`단지` 매칭일 때만 건물 지표로 인용한다.
- 건축물대장은 **입력한 지번 필지** 기준이다. 아파트 단지처럼 여러 필지면 시세 매칭(단지명)과 대장(필지)이 다른 건물을 가리킬 수 있음을 알려라.
- 건축물대장 섹션이 "확인 불가"면 이렇게 안내한다: "건축물 용도(근생 여부)는 이번 진단에서 확인하지 못했습니다. 정부24에서 건축물대장을 무료 열람하거나, DATA_GO_KR_API_KEY를 설정하면 자동 확인됩니다."
- 리포트 하단 면책("공공데이터 기반 참고 자료이며 계약 판단의 근거가 아닙니다")을 생략하지 말 것.

## Watchdog (계약 후 감시)

두 경로를 안내한다:

- **GitHub fork (권장, 로컬 상시 실행 불필요)**: https://github.com/Pakkoc/jeonse-guard 를 fork → Actions 활성화 + Issues 켜기 → `watchlist.toml`에 내 계약 정보 커밋 → 매일 07:00 KST 자동 감시, 변동 시 Issue 알림.
- **로컬**: 클론한 레포에서 `watchlist.example.toml`을 `watchlist.toml`로 복사·수정 후 `$JG watch` 를 cron 등으로 주기 실행.

## Failure modes

- `다중 후보` / `필지 정보 없음` → 주소가 하나로 확정되지 않음. 후보 목록을 사용자에게 보여주고 정확한 지번을 받아 재시도.
- 건축물대장 `HTTP 401` → `DATA_GO_KR_API_KEY` 미설정이거나 발급 직후 활성화 지연(수분~1시간). 키 발급: data.go.kr에서 데이터셋 15134735 활용신청(자동승인) 후 마이페이지 일반 인증키 사용.
- `no_price_reference` 신호 → 매매 표본 3건 미만(빌라에서 흔함). 전세가율 신뢰도가 낮다는 뜻이며, 이것 자체가 "시세 확인이 어려운 매물"이라는 정보임을 설명.
- 프록시 503/타임아웃 → 잠시 후 재시도. 반복되면 섹션 강등된 리포트라도 체크리스트와 함께 전달.

## Done when

- scan이 리포트를 생성했고, 전세가율(또는 산출 불가 사유)·검토 신호·직접 확인 체크리스트·면책을 사용자에게 전달했다.
- 단정 표현 없이 사실과 근거로만 설명했다.
- 계약 진행 의사가 있는 사용자에게 워치독 설정 경로를 안내했다.
